Generated by `sh tools/maps.sh RETAIL.exe JPRE.exe OEM.exe [JP.exe]`: the vomap.py
function maps (`*.pkl`) and the resolution patch's port tables or FAIL lists
(`*_port.txt`). Everything here derives from the game executables and is
ignored by git; this note is the only tracked file. docs/HIRES.md explains
what the tables are and what a FAIL list means.
